import { useEffect } from 'react'

export const useInit = () => {
  useEffect(() => {
    /*
      TODO: loading some data here
      For example, load user saved data from local storage
    */
  }, [])

  return '💩'
}
